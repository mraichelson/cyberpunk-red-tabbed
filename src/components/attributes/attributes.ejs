<div class="section-attributes">
  <div class="sheet-columns">
    <% attributes.forEach(function(attribute) { %>
    <div class="sheet-column">
      <span class="accent-font"><%= attribute.name %></span>
      <input type="number" name="attr_<%= attribute.field %>" />
    </div>
    <% }) %>
  </div>
</div>
